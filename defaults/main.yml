---
APP_PYTHON: "{{ '/usr/bin/python' if lookup('file', '/usr/bin/python', errors='ignore') else '/usr/bin/python3' }}"
APP_NAME: bzflag
APP_DISPLAY_NAME: BZ Flag
APP_COMMENT: BZ Flag
APP_USER: "{{ lookup('env', 'USER') }}"
APP_GROUP: "{{ APP_USER }}"
APP_SRC: "{{ lookup('env', 'HOME') }}/.local/src/{{ APP_NAME  }}"
APP_REPO: git@github.com:BZFlag-Dev/bzflag.git
APP_TAG: v2.4.22

